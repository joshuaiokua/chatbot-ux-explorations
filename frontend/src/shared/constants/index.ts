/**
 * Frontend Constants
 * @module frontend/src/shared/constants
 * @description This module holds all the constants used across the application's frontend.
 */

import process from "process";

export const baseURL = process.env.BACKEND_URL || "http://localhost:3001";
